import { Routes } from '@angular/router';
import { Facturacion } from './facturacion/facturacion';

export const routes: Routes = [
  { path: 'facturas', component: Facturacion },
  // Puedes añadir más rutas aquí para otros componentes
];